# Environment Configuration - Backend

# ===========================
# Database Configuration
# ===========================
DB_PATH=./data/inventory.db
# Database file location (will be created if not exists)
# Production: Use absolute path outside web root

# ===========================
# Server Configuration
# ===========================
PORT=3001
# Backend server port

NODE_ENV=development
# Environment mode: development | production | test

API_VERSION=v1
# API version prefix (used in /api/v1/...)

# ===========================
# Security Configuration
# ===========================
CORS_ORIGIN=http://localhost:5173
# Allowed frontend origin (comma-separated for multiple)
# Production: Use actual frontend domain

RATE_LIMIT_WINDOW_MS=60000
# Rate limit time window in milliseconds (60000 = 1 minute)

RATE_LIMIT_MAX_REQUESTS=100
# Maximum requests per IP per time window

# ===========================
# Logging Configuration
# ===========================
LOG_LEVEL=info
# Logging level: error | warn | info | debug
# Production: Use 'error' or 'warn'

# ===========================
# Database Performance
# ===========================
DB_CACHE_SIZE=-64000
# SQLite cache size in KB (negative for KB, positive for pages)
# -64000 = 64MB cache

DB_JOURNAL_MODE=WAL
# SQLite journal mode: WAL (Write-Ahead Logging) recommended
# WAL provides better concurrency

# ===========================
# Notes
# ===========================
# - Never commit the actual .env file to version control
# - Copy this file to .env and customize values
# - Use absolute paths in production environments
# - Adjust CORS_ORIGIN for production deployment
